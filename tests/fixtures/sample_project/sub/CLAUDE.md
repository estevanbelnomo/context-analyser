# Sub-directory Instructions

This nested CLAUDE.md applies only when Claude is working inside the sub/
directory. The scanner must classify it as a conditional source, so it is
NEVER part of the resting baseline of the project root.

## Local Rules
- These rules layer on top of the root CLAUDE.md.
- They activate only for files under this directory.
