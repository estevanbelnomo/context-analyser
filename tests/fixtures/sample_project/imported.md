# Imported Conventions

This file is pulled in by the root CLAUDE.md via an @import line. It exists to
prove the scanner resolves imports recursively and counts each resolved target
as its own always-loaded manifest entry under the import category.

## Conventions
- Document every public function with a one-line summary.
- Validate all external input before use.
- Never write files from a read-only tool.
